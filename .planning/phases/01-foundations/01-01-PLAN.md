---
phase: 01-foundations
plan: 01
type: execute
wave: 1
depends_on: [01-00]
files_modified:
  - client-mod/build.gradle.kts
  - client-mod/settings.gradle.kts
  - client-mod/gradle.properties
  - client-mod/gradle/wrapper/gradle-wrapper.properties
  - client-mod/gradle/wrapper/gradle-wrapper.jar
  - client-mod/gradlew
  - client-mod/gradlew.bat
  - client-mod/.gitignore
  - client-mod/README.md
  - client-mod/src/main/java/club/wiiwho/WiiWho.java
  - client-mod/src/main/resources/mcmod.info
  - client-mod/src/main/resources/mixins.wiiwho.json
  - client-mod/src/test/java/club/wiiwho/ModidTest.java
  - docs/ANTICHEAT-SAFETY.md
autonomous: false
requirements:
  - MOD-01
  - MOD-03

must_haves:
  truths:
    - "MODID `wiiwho` has been collision-checked against Modrinth and CurseForge (manual browser check for CF), with the result recorded in ANTICHEAT-SAFETY.md or STATE.md"
    - "`client-mod/` directory exists with a Forge 1.8.9 Gradle project using gg.essential.loom 0.10.0.+ and Gradle 8.8"
    - "Running `./gradlew --dry-run build` from `client-mod/` completes without errors (MOD-01 smoke)"
    - "Java class `club.wiiwho.WiiWho` exists with `public static final String MODID = \"wiiwho\";` (MOD-03 compile-time assertion)"
    - "JUnit test `ModidTest` passes — `./gradlew :client-mod:test --tests club.wiiwho.ModidTest` exits 0"
    - "Template's git history has been stripped — no .git from nea89o/Forge1.8.9Template in `client-mod/`"
  artifacts:
    - path: "client-mod/build.gradle.kts"
      provides: "Loom + Mixin + shadow + pack200 plugin stack pinned to verified template versions"
      contains: "gg.essential.loom"
    - path: "client-mod/settings.gradle.kts"
      provides: "pluginManagement + loom resolutionStrategy + rootProject.name"
      contains: "wiiwho-client-mod"
    - path: "client-mod/gradle.properties"
      provides: "modid, baseGroup, mcVersion, version pins"
      contains: "modid=wiiwho"
    - path: "client-mod/gradle/wrapper/gradle-wrapper.properties"
      provides: "Gradle 8.8 wrapper pin per RESEARCH.md Pitfall 1"
      contains: "gradle-8.8-bin.zip"
    - path: "client-mod/src/main/java/club/wiiwho/WiiWho.java"
      provides: "@Mod annotated entry class with MODID literal"
      contains: 'public static final String MODID = "wiiwho"'
    - path: "client-mod/src/test/java/club/wiiwho/ModidTest.java"
      provides: "JUnit 5 assertion that MODID === \"wiiwho\""
      contains: "@Test"
    - path: "client-mod/src/main/resources/mcmod.info"
      provides: "Forge modid/version/name metadata"
      contains: '"modid": "wiiwho"'
    - path: "client-mod/src/main/resources/mixins.wiiwho.json"
      provides: "Mixin config declaring the club.wiiwho.mixins package"
      contains: '"package": "club.wiiwho.mixins"'
  key_links:
    - from: "client-mod/build.gradle.kts"
      to: "client-mod/src/main/resources/mixins.wiiwho.json"
      via: "loom.forge.mixinConfig registration"
      pattern: 'mixinConfig\("mixins.wiiwho.json"\)'
    - from: "client-mod/src/main/java/club/wiiwho/WiiWho.java"
      to: "client-mod/src/main/resources/mcmod.info"
      via: "@Mod(modid=...) string equals mcmod.info modid"
      pattern: '"modid": "wiiwho"'
    - from: "client-mod/src/main/resources/mixins.wiiwho.json"
      to: "client-mod/src/main/java/club/wiiwho/mixins/"
      via: "package declaration — mixins loaded from this package"
      pattern: 'club\.wiiwho\.mixins'
    - from: "client-mod/gradle.properties"
      to: "client-mod/build.gradle.kts"
      via: "modid property read by build script for mcmod.info templating + mixin refmap naming"
      pattern: "modid=wiiwho"
---

<objective>
Scaffold the Forge 1.8.9 mod as `client-mod/` at the repo root. Copy the verified scaffolding from `nea89o/Forge1.8.9Template` (Gradle 8.8, gg.essential.loom 0.10.0.+, Mixin 0.7.11-SNAPSHOT runtime + 0.8.5-SNAPSHOT annotation processor, DevAuth-forge-legacy 1.2.1, Forge 11.15.1.2318-1.8.9, MCP stable_22), rename package/group/modid to `club.wiiwho` / `wiiwho`, commit the MODID collision check result to ANTICHEAT-SAFETY.md, and leave `./gradlew --dry-run build` passing.

This plan covers MOD-01 (scaffold exists with modern toolchain) and MOD-03 (MODID is generic, non-feature-descriptive, collision-checked). It does NOT run `runClient` (that is Plan 02's job) and does NOT write the trivial Mixin implementation (also Plan 02).

Purpose: Establishing the build system correctly is the highest-risk single task in Phase 1 — RESEARCH.md Pitfall 1 and STACK.md warn of a week-long timesink if the toolchain drifts. Using the live template's verified pins protects against every common 1.8.9 Gradle failure mode.

Output: A buildable `client-mod/` Gradle project with the `@Mod` entry class, mcmod.info, mixin config (class list intentionally empty or containing just the placeholder name to satisfy the JSON contract; the MixinMinecraft class itself is added in Plan 02), and a JUnit test asserting MODID.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundations/01-CONTEXT.md
@.planning/phases/01-foundations/01-RESEARCH.md
@.planning/phases/01-foundations/01-VALIDATION.md
@.planning/phases/01-foundations/01-00-SUMMARY.md
@CLAUDE.md
@docs/ANTICHEAT-SAFETY.md

<interfaces>
<!-- Key contracts + external references. Executor uses these directly — no codebase exploration needed. -->

Reference template (to clone + strip history): https://github.com/nea89o/Forge1.8.9Template
  - Verified versions 2026-04-20:
    - Gradle wrapper: `gradle-8.8-bin.zip`
    - Plugins: `id("idea")`, `id("java")`, `id("gg.essential.loom") version "0.10.0.+"`, `id("dev.architectury.architectury-pack200") version "0.1.3"`, `id("com.github.johnrengelman.shadow") version "8.1.1"`
    - Repositories: mavenCentral, https://repo.spongepowered.org/maven, https://pkgs.dev.azure.com/djtheredstoner/DevAuth/_packaging/public/maven/v1
    - Dependencies (key):
      - `minecraft("com.mojang:minecraft:1.8.9")`
      - `mappings("de.oceanlabs.mcp:mcp_stable:22-1.8.9")`
      - `forge("net.minecraftforge:forge:1.8.9-11.15.1.2318-1.8.9")`
      - `shadowImpl("org.spongepowered:mixin:0.7.11-SNAPSHOT") { isTransitive = false /* exclude group = "org.apache.logging.log4j", module = "log4j-core" */ }`
      - `annotationProcessor("org.spongepowered:mixin:0.8.5-SNAPSHOT:processor")`
      - `runtimeOnly("me.djtheredstoner:DevAuth-forge-legacy:1.2.1")`

Rename points (from RESEARCH.md §Mod Scaffold Playbook step 2):
  - `gradle.properties`:
    - `loom.platform=forge`
    - `org.gradle.jvmargs=-Xmx2g`
    - `baseGroup=club.wiiwho`
    - `mcVersion=1.8.9`
    - `modid=wiiwho`
    - `version=0.1.0-SNAPSHOT`
  - `settings.gradle.kts`: `rootProject.name = "wiiwho-client-mod"`
  - Package directory: move `src/main/java/<template-default>` to `src/main/java/club/wiiwho/`
  - Update `mixins.wiiwho.json` `"package"` field to `"club.wiiwho.mixins"`
  - Update `@Mod(modid = ...)` to `modid = WiiWho.MODID` where `MODID = "wiiwho"`

WiiWho.java contract (per RESEARCH.md §Mod Scaffold Playbook step 3):
```java
package club.wiiwho;

import net.minecraftforge.fml.common.Mod;
import net.minecraftforge.fml.common.event.FMLInitializationEvent;
import net.minecraftforge.fml.common.event.FMLPreInitializationEvent;
import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

@Mod(modid = WiiWho.MODID, version = WiiWho.VERSION, name = "WiiWho Client", clientSideOnly = true)
public class WiiWho {
    public static final String MODID = "wiiwho";
    public static final String VERSION = "0.1.0";

    private static final Logger LOGGER = LogManager.getLogger(MODID);

    @Mod.EventHandler
    public void preInit(FMLPreInitializationEvent event) {
        LOGGER.info("WiiWho preInit — v{}", VERSION);
    }

    @Mod.EventHandler
    public void init(FMLInitializationEvent event) {
        LOGGER.info("WiiWho init — ready");
    }
}
```
Critical: `clientSideOnly = true` (1.8.9 Forge requires this for client-only mods).

mcmod.info contract:
```json
[
  {
    "modid": "wiiwho",
    "name": "WiiWho Client",
    "description": "A performance + QoL Minecraft 1.8.9 client.",
    "version": "${version}",
    "mcversion": "${mcversion}",
    "url": "",
    "authors": ["WiiWho Client team"],
    "credits": ""
  }
]
```

mixins.wiiwho.json contract (Plan 02 adds MixinMinecraft to `client` array — Plan 01 leaves `client: []`):
```json
{
  "required": true,
  "package": "club.wiiwho.mixins",
  "compatibilityLevel": "JAVA_8",
  "refmap": "mixins.wiiwho.refmap.json",
  "minVersion": "0.7.11",
  "client": [],
  "injectors": { "defaultRequire": 1 }
}
```

ModidTest.java contract (JUnit 5):
```java
package club.wiiwho;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertEquals;

class ModidTest {
    @Test
    void modidIsWiiwho() {
        assertEquals("wiiwho", WiiWho.MODID, "MOD-03: MODID must be the generic, non-feature-descriptive 'wiiwho'");
    }
}
```

MODID collision check URLs (per RESEARCH.md §MODID Collision Check):
  - https://modrinth.com/mods?q=wiiwho (Modrinth — automated clean as of 2026-04-20)
  - https://www.curseforge.com/minecraft/search?search=wiiwho (CurseForge — MANUAL BROWSER CHECK required; returns 403 to automated fetch)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: MODID collision check (owner performs browser check + confirms)</name>
  <read_first>
    - .planning/phases/01-foundations/01-RESEARCH.md (MODID Collision Check section)
    - .planning/phases/01-foundations/01-CONTEXT.md (D-02 — escalation path if collision found)
    - docs/ANTICHEAT-SAFETY.md (first row references MODID `wiiwho`; this checkpoint confirms the collision-clean status that row declares)
  </read_first>
  <files>(none — this is a manual checkpoint; see content below and resume-signal for outputs owner records to STATE.md)</files>
  <action>
    This is a checkpoint task. The actual instructions for the executor/owner are in the subsequent &lt;what-built&gt; / &lt;how-to-verify&gt; / &lt;instructions&gt; elements and &lt;resume-signal&gt;. The executor should:
    1. Read all &lt;read_first&gt; files above before proceeding.
    2. Present the checkpoint content to the owner (what was built, how to verify, acceptance criteria, resume signal).
    3. Block until the owner types the resume signal.
    4. Record any owner-reported evidence (client IDs, timestamps, GUIDs, etc.) per the acceptance_criteria into .planning/STATE.md as instructed.
    See the checkpoint-specific elements below for the full protocol.
  </action>
  <verify>
    <automated>test -n "$RESUME_SIGNAL" # Checkpoint tasks verify via resume-signal below, not automated commands. Executor MUST wait for owner input before marking this task done. See &lt;acceptance_criteria&gt; below for the verifiable post-conditions.</automated>
  </verify>
  <what-built>
    No code changes. This is a pre-flight manual verification gate: confirm the MODID `wiiwho` is not already claimed on CurseForge (automated fetch in research returned 403, so this one has to be a browser check). Modrinth is already confirmed clean (research 2026-04-20).
  </what-built>
  <how-to-verify>
    1. Open in a browser: `https://www.curseforge.com/minecraft/search?search=wiiwho`
    2. Visually scan the results page. "Clean" means: zero mods titled exactly `wiiwho` or with a slug `/mc-mods/wiiwho`. Near-matches like `wiiwho-something` are NOT collisions but should be noted.
    3. As a second sanity check, open `https://www.curseforge.com/minecraft/mc-mods/wiiwho` directly — a 404 page means no mod claims that exact slug; a mod detail page means there's a collision.
    4. (Already done in research but re-confirm) Open `https://modrinth.com/mods?q=wiiwho` — confirm still zero results.
    5. Record the result in the resume signal.
  </how-to-verify>
  <acceptance_criteria>
    - Owner has personally opened the CurseForge search URL in a browser and confirmed zero exact-name or exact-slug matches
    - Owner has re-confirmed Modrinth shows zero results
    - Owner has either approved "proceed with `wiiwho`" OR escalated to one of the D-02 fallback names (`wiiwho-client`, `wiiwhocl`, `wiiwho_client`) — in which case that fallback name is used consistently for the rest of this plan and subsequent plans
    - The approval (or escalation decision) is logged in `.planning/STATE.md` under Accumulated Context → Decisions with today's date
  </acceptance_criteria>
  <resume-signal>
    Type one of:
    - `approved wiiwho` — CurseForge + Modrinth clean, proceed with MODID `wiiwho`
    - `escalate to <new-id>` — collision found; use `<new-id>` (one of `wiiwho-client`, `wiiwhocl`, `wiiwho_client`, or an owner-chosen alternative) for ALL subsequent tasks and files
    - `blocked: <reason>` — cannot proceed; describe the blocker
  </resume-signal>
  <done>
    Owner has confirmed the MODID decision. If `escalate to <new-id>` was chosen, Task 2 below uses that ID instead of `wiiwho` in every file. All subsequent Phase 1 plans must also use the chosen ID.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold client-mod from template + rename to club.wiiwho + write @Mod entry + ModidTest</name>
  <files>client-mod/build.gradle.kts, client-mod/settings.gradle.kts, client-mod/gradle.properties, client-mod/gradle/wrapper/gradle-wrapper.properties, client-mod/gradle/wrapper/gradle-wrapper.jar, client-mod/gradlew, client-mod/gradlew.bat, client-mod/.gitignore, client-mod/README.md, client-mod/src/main/java/club/wiiwho/WiiWho.java, client-mod/src/main/resources/mcmod.info, client-mod/src/main/resources/mixins.wiiwho.json, client-mod/src/test/java/club/wiiwho/ModidTest.java, docs/ANTICHEAT-SAFETY.md</files>
  <read_first>
    - .planning/phases/01-foundations/01-RESEARCH.md (Mod Scaffold Playbook steps 1-5; Pitfall 1 re Gradle 8.8; Pitfall 3 re package rename)
    - .planning/phases/01-foundations/01-CONTEXT.md (D-02, D-06, D-27)
    - CLAUDE.md (repo structure section — confirms `client-mod/` sibling placement)
    - Template repo https://github.com/nea89o/Forge1.8.9Template — fetch raw file contents of `build.gradle.kts`, `settings.gradle.kts`, `gradle.properties`, `gradle-wrapper.properties` at master branch before copying (verify versions have not moved since research)
    - docs/ANTICHEAT-SAFETY.md (first-row MODID signoff — update the row if escalation occurred in Task 1)
  </read_first>
  <behavior>
    - Test 1: `client-mod/` directory exists with the full Gradle project layout (gradlew, gradle/wrapper/, build.gradle.kts, etc.)
    - Test 2: Template's .git has been stripped — `test -d client-mod/.git` returns false (D-27: do not fork wholesale)
    - Test 3: `gradle-wrapper.properties` declares Gradle 8.8 (RESEARCH Pitfall 1)
    - Test 4: `gradle.properties` has `modid=wiiwho` (or escalated ID from Task 1), `baseGroup=club.wiiwho`, `mcVersion=1.8.9`
    - Test 5: `WiiWho.java` compiles standalone (JUnit test asserts `MODID.equals("wiiwho")`)
    - Test 6: `./gradlew :client-mod:test --tests club.wiiwho.ModidTest` exits 0 (MOD-03 automated assertion)
    - Test 7: `./gradlew --dry-run build` from `client-mod/` exits 0 (MOD-01 smoke)
    - Test 8: `grep -rn "com.example\|examplemod" client-mod/` returns no results (rename is complete per RESEARCH Pitfall 3)
  </behavior>
  <action>
    **If Task 1 returned `escalate to <new-id>`, substitute `<new-id>` for every literal `wiiwho` below in MODID / path positions. Keep `WiiWho` (capital-W class name) and `club.wiiwho` package (since the escalation is only on the MODID, not the brand name). However, if the escalated ID contains a hyphen (e.g. `wiiwho-client`), rename the mixin config filename to `mixins.<new-id-with-underscores>.json` and adjust `baseGroup` accordingly if needed (MODIDs with hyphens are allowed in gradle.properties but JSON filenames should avoid them).**

    **Step A — Clone template and strip history:**
    From repo root on Windows (bash shell):
    ```bash
    git clone https://github.com/nea89o/Forge1.8.9Template client-mod
    rm -rf client-mod/.git
    ```
    This leaves `client-mod/` as a plain directory under the wiiwho-client git repo, per D-27.

    **Step B — Verify template versions haven't drifted:**
    Open the following files inside `client-mod/` and confirm these exact values; if any drift is detected, record the new value in the commit message but DO NOT downgrade:
    - `gradle/wrapper/gradle-wrapper.properties`: `distributionUrl=https\://services.gradle.org/distributions/gradle-8.8-bin.zip`
    - `build.gradle.kts`: plugin block contains `id("gg.essential.loom") version "0.10.0.+"`, `id("dev.architectury.architectury-pack200") version "0.1.3"`, `id("com.github.johnrengelman.shadow") version "8.1.1"`
    - `build.gradle.kts` dependency block contains `shadowImpl("org.spongepowered:mixin:0.7.11-SNAPSHOT")` AND `annotationProcessor("org.spongepowered:mixin:0.8.5-SNAPSHOT:processor")` (both — this is RESEARCH Pitfall 6)
    - `build.gradle.kts` dependency block contains `runtimeOnly("me.djtheredstoner:DevAuth-forge-legacy:1.2.1")`

    **Step C — Rename points in config files:**

    Edit `client-mod/gradle.properties` to contain (preserving any other properties the template ships):
    ```properties
    loom.platform=forge
    org.gradle.jvmargs=-Xmx2g
    baseGroup=club.wiiwho
    mcVersion=1.8.9
    modid=wiiwho
    version=0.1.0-SNAPSHOT
    ```

    Edit `client-mod/settings.gradle.kts`: set `rootProject.name = "wiiwho-client-mod"` (replace whatever the template uses).

    In `client-mod/build.gradle.kts`, ensure the `group` / `archivesBaseName` (or equivalent Kotlin-DSL properties) are set to the `baseGroup` and `modid` properties from gradle.properties. If the template already uses property substitution (e.g. `group = project.property("baseGroup") as String`), leave the wiring as-is; only change if the template hardcodes a value.

    Confirm the template's `loom { forge { mixinConfig("mixins.${project.property("modid")}.json") } }` (or equivalent) will resolve to `mixins.wiiwho.json` — adjust the Mixin config filename in Step E accordingly.

    **Step D — Rename Java package directory:**

    Delete the template's default example package (e.g. `client-mod/src/main/java/com/example/examplemod/` or whatever the template ships). Create the new structure:
    - `client-mod/src/main/java/club/wiiwho/` (this is where WiiWho.java goes in Step F)
    - `client-mod/src/main/java/club/wiiwho/mixins/` (empty in Plan 01; Plan 02 adds MixinMinecraft.java)

    Grep the entire `client-mod/` tree for references to the old package name (`com.example.examplemod` or similar) and remove/update every one. RESEARCH Pitfall 3 specifically warns about orphaned package references in `mixins.*.json`, `@Mod` annotations, and `mcmod.info` causing silent Mixin load failure.

    **Step E — Write mixins.wiiwho.json:**

    Write `client-mod/src/main/resources/mixins.wiiwho.json` (replace any template-provided file):
    ```json
    {
      "required": true,
      "package": "club.wiiwho.mixins",
      "compatibilityLevel": "JAVA_8",
      "refmap": "mixins.wiiwho.refmap.json",
      "minVersion": "0.7.11",
      "client": [],
      "injectors": { "defaultRequire": 1 }
    }
    ```
    `"client": []` is intentionally empty in Plan 01. Plan 02 adds `"MixinMinecraft"` to this array.

    **Step F — Write WiiWho.java:**

    Write `client-mod/src/main/java/club/wiiwho/WiiWho.java` verbatim per the interfaces block above (with `clientSideOnly = true`).

    **Step G — Write mcmod.info:**

    Write `client-mod/src/main/resources/mcmod.info` verbatim per the interfaces block above.

    **Step H — Write ModidTest.java:**

    Write `client-mod/src/test/java/club/wiiwho/ModidTest.java` verbatim per the interfaces block above.

    If the template's `build.gradle.kts` does not include JUnit 5 test configuration, add:
    ```kotlin
    dependencies {
        testImplementation("org.junit.jupiter:junit-jupiter:5.10.2")
    }
    tasks.test { useJUnitPlatform() }
    ```
    (Place these after the existing dependency / task blocks.)

    **Step I — Write client-mod/README.md:**

    Create `client-mod/README.md` with:
    ```markdown
    # WiiWho Client — Forge 1.8.9 Mod

    Built from [nea89o/Forge1.8.9Template](https://github.com/nea89o/Forge1.8.9Template) per decision D-27. History stripped on initial scaffold.

    ## Requirements

    - JDK 8 (Temurin) — compile target for Minecraft 1.8.9
    - JDK 17 (Temurin) — Gradle daemon
    - Gradle 8.8 (via wrapper — `./gradlew`)

    ## Common tasks

    - `./gradlew --dry-run build` — verify configuration
    - `./gradlew :client-mod:test` — run unit tests
    - `./gradlew runClient -Ddevauth.enabled=1` — launch dev 1.8.9 with the mod + DevAuth MS login (requires owner-owned MS account on first run)
    - `./gradlew build` — produce release jar (Phase 4 wires this into the launcher's injection step)

    ## Tip

    Add `systemProp.devauth.enabled=1` to `~/.gradle/gradle.properties` (user-global, NOT committed) if you want DevAuth to always be on for this workspace.
    ```

    **Step J — .gitignore:**

    Ensure `client-mod/.gitignore` contains at minimum (append if template provides partial):
    ```
    # Gradle
    .gradle/
    build/
    out/

    # IDEs
    .idea/
    *.iml
    *.ipr
    *.iws
    .vscode/
    .settings/
    .classpath
    .project

    # Loom
    run/
    ```

    **Step K — Update ANTICHEAT-SAFETY.md row if MODID escalated:**

    If Task 1 returned `escalate to <new-id>`, edit the first signoff row in `docs/ANTICHEAT-SAFETY.md` to replace `Project MODID: \`wiiwho\`` with `Project MODID: \`<new-id>\``. If Task 1 returned `approved wiiwho`, leave the ANTICHEAT-SAFETY.md row as-is. Also update the ANTICHEAT-SAFETY.md row's "What it reads / writes" column to note the collision-check result: "CurseForge manual browser check 2026-04-20: clean. Modrinth automated check 2026-04-20: clean."

    **Step L — Verify.**

    Run from `client-mod/`:
    ```bash
    ./gradlew --version                       # expect Gradle 8.8
    ./gradlew --dry-run build                  # expect: BUILD SUCCESSFUL
    ./gradlew :client-mod:test --tests club.wiiwho.ModidTest   # expect: 1 passed, 0 failed
    ```

    Record all three command outputs in the commit message.
  </action>
  <verify>
    <automated>cd client-mod && ./gradlew --dry-run build && ./gradlew :client-mod:test --tests club.wiiwho.ModidTest && cd .. && ! grep -rn "com.example.examplemod\|examplemod" client-mod/ --include="*.java" --include="*.kts" --include="*.properties" --include="*.json" 2>/dev/null && grep -q "gradle-8.8-bin.zip" client-mod/gradle/wrapper/gradle-wrapper.properties && grep -q 'modid=wiiwho' client-mod/gradle.properties && grep -q 'public static final String MODID = "wiiwho"' client-mod/src/main/java/club/wiiwho/WiiWho.java</automated>
  </verify>
  <acceptance_criteria>
    - `client-mod/` directory exists
    - `test ! -d client-mod/.git` (template history stripped per D-27)
    - `grep -q "gradle-8.8-bin.zip" client-mod/gradle/wrapper/gradle-wrapper.properties` succeeds
    - `grep -q 'id("gg.essential.loom") version "0.10.0.+"' client-mod/build.gradle.kts` succeeds (plugin pin)
    - `grep -q 'shadowImpl("org.spongepowered:mixin:0.7.11-SNAPSHOT"' client-mod/build.gradle.kts` succeeds (Mixin runtime pin)
    - `grep -q 'annotationProcessor("org.spongepowered:mixin:0.8.5-SNAPSHOT:processor")' client-mod/build.gradle.kts` succeeds (Mixin AP pin per Pitfall 6)
    - `grep -q 'runtimeOnly("me.djtheredstoner:DevAuth-forge-legacy:1.2.1")' client-mod/build.gradle.kts` succeeds
    - `grep -q "modid=wiiwho" client-mod/gradle.properties` succeeds (or escalated value if Task 1 chose one)
    - `grep -q "baseGroup=club.wiiwho" client-mod/gradle.properties` succeeds
    - `grep -q 'rootProject.name = "wiiwho-client-mod"' client-mod/settings.gradle.kts` succeeds
    - File `client-mod/src/main/java/club/wiiwho/WiiWho.java` exists and contains `public static final String MODID = "wiiwho"` AND `clientSideOnly = true`
    - File `client-mod/src/main/resources/mcmod.info` exists and contains `"modid": "wiiwho"`
    - File `client-mod/src/main/resources/mixins.wiiwho.json` exists with `"package": "club.wiiwho.mixins"` and `"client": []`
    - File `client-mod/src/test/java/club/wiiwho/ModidTest.java` exists with `@Test` annotation
    - `grep -rn "com.example\|examplemod" client-mod/` returns zero matches (rename fully applied per Pitfall 3)
    - `cd client-mod && ./gradlew --dry-run build` exits 0 (MOD-01 smoke)
    - `cd client-mod && ./gradlew :client-mod:test --tests club.wiiwho.ModidTest` exits 0 (MOD-03 automated)
    - `docs/ANTICHEAT-SAFETY.md` first signoff row updated with CurseForge/Modrinth collision-check result and date
  </acceptance_criteria>
  <done>
    `client-mod/` is a buildable Forge 1.8.9 Gradle project using the modern community toolchain (gg.essential.loom 0.10.0.+ on Gradle 8.8, Mixin 0.7.11-SNAPSHOT runtime + 0.8.5-SNAPSHOT AP, DevAuth 1.2.1). MODID `wiiwho` is enshrined in code, resources, and the anticheat-safety log. ModidTest passes. The mod has no implementation logic yet and Mixin has no target class — that's Plan 02's scope.
  </done>
</task>

</tasks>

<verification>
- Task 1: Owner types `approved wiiwho` or `escalate to <new-id>` — gate passes
- Task 2 automated: `./gradlew --dry-run build` and `./gradlew :client-mod:test --tests club.wiiwho.ModidTest` both exit 0
- Manual spot-check: Open `client-mod/src/main/java/club/wiiwho/WiiWho.java` in an editor — confirm `@Mod` annotation, `clientSideOnly = true`, and MODID literal are all present
- Manual spot-check: Open `docs/ANTICHEAT-SAFETY.md` — confirm first row reflects the collision-check outcome
</verification>

<success_criteria>
- [ ] MODID collision checked on both Modrinth + CurseForge (manual)
- [ ] `client-mod/` scaffolded with Gradle 8.8 + gg.essential.loom 0.10.0.+
- [ ] Template git history stripped
- [ ] Package renamed from template default to `club.wiiwho`
- [ ] @Mod entry class exists with correct MODID + clientSideOnly
- [ ] mcmod.info + mixins.wiiwho.json reference correct modid/package
- [ ] ModidTest.java JUnit 5 test passes
- [ ] `./gradlew --dry-run build` passes (MOD-01)
- [ ] `./gradlew :client-mod:test` passes (MOD-03 automated)
- [ ] docs/ANTICHEAT-SAFETY.md first row updated with CF/Modrinth clean notation
- [ ] All changes committed in a single commit `feat(01): scaffold client-mod from template + MODID collision check`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations/01-01-SUMMARY.md` per the summary template. MUST include:
- MODID collision-check outcome (approved OR escalated, with the URL screenshots or text evidence)
- The exact Gradle/loom/Mixin/DevAuth/Forge versions pinned (in case template drifts)
- The `./gradlew --dry-run build` and `./gradlew :client-mod:test` output
- Any rename gotchas encountered (template may have had `com.example.examplemod` or some other default)
- Confirmation that `mixins.wiiwho.json` has `"client": []` (Plan 02 adds MixinMinecraft)
</output>
