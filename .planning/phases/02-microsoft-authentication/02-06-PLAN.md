---
phase: 02-microsoft-authentication
plan: 06
type: execute
wave: 5
depends_on: [02-05]
files_modified:
  - docs/MANUAL-QA-auth.md
  - docs/azure-app-registration.md
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06]
must_haves:
  truths:
    - "End-to-end live Microsoft login succeeds — user reaches Play-forward with real MS account"
    - "Filesystem audit finds zero token substrings in %APPDATA%/Wiiwho (AUTH-04 Success Criterion 2)"
    - "At least one reproducible XSTS error surfaces with locked D-10 copy (AUTH-03)"
    - "Logout returns to LoginScreen and deletes token files; re-login succeeds (AUTH-06)"
    - "Stop signing in / ESC cancellation path works mid-flow (D-07)"
    - "Silent refresh on subsequent launcher open lands directly in Play-forward (D-02, AUTH-02 SC2)"
  artifacts:
    - path: "docs/MANUAL-QA-auth.md"
      provides: "Sign-off table populated with date + result for each of the 6 test cases"
    - path: "docs/azure-app-registration.md"
      provides: "MCE approval date recorded (prerequisite for execution)"
  key_links:
    - from: "docs/MANUAL-QA-auth.md"
      to: "live Microsoft endpoints (login.microsoftonline.com, user.auth.xboxlive.com, xsts.auth.xboxlive.com, api.minecraftservices.com)"
      via: "pnpm --filter ./launcher dev session + system browser"
      pattern: "Microsoft"
---

<objective>
Human-verify the full live-endpoint Microsoft authentication stack against real Microsoft APIs with a real Microsoft account. This is the phase gate — no amount of unit+integration testing can substitute for the MCE-gated `/authentication/login_with_xbox` call succeeding in production.

Purpose: Closes AUTH-01 (real MSAL device code flow), AUTH-02 Success Criterion 2 (7-day refresh — optional within phase, can overlap into Phase 3), AUTH-03 (at least one XSTS error reproduced with locked copy), AUTH-04 (filesystem token-leak audit), AUTH-05 (username + UUID visible), AUTH-06 (logout + re-login).
Output: MANUAL-QA-auth.md sign-off table populated; azure-app-registration.md updated with the MCE approval date; any defects found logged as issues and routed back to a gap-closure plan.
</objective>

<execution_context>
@C:/Users/Eliyahu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eliyahu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-microsoft-authentication/02-CONTEXT.md
@.planning/phases/02-microsoft-authentication/02-UI-SPEC.md
@.planning/phases/02-microsoft-authentication/02-VALIDATION.md
@docs/MANUAL-QA-auth.md
@docs/azure-app-registration.md

<interfaces>
**Prerequisite gate (BLOCKING):**

The Azure AD app `60cbce02-072b-4963-833d-edb6f5badc2a` was submitted to Microsoft's MCE (Minecraft API permission) review queue on 2026-04-20. Expected return window: 2026-04-21 to 2026-04-27. Until the approval email arrives, the final `/authentication/login_with_xbox` step will return an error and **Test 1 (happy path login) will fail regardless of code quality**. RESEARCH.md Pitfall 9 covers this explicitly.

**First step in this plan:** verify the approval is received. If not received, this plan cannot execute — pause, wait for the email, update `docs/azure-app-registration.md` with the approval-received date, then resume.

**Approval indicators (confirm at least one):**
1. Email from Microsoft Partner Center / MCE reviewer stating the app is approved for `api.minecraftservices.com` access
2. A successful live test call against `api.minecraftservices.com/authentication/login_with_xbox` with our Azure AD client ID (can be done via `curl` once the prior XSTS chain is available — but this IS the test we're trying to run, so the email is the primary signal)
3. Azure Portal app registration shows no pending-review banner under API permissions

If uncertainty remains after the expected window, owner contacts the MCE review team using the thread from the original submission (link: `https://aka.ms/mce-reviewappid` / the email thread).

**Execution environment:**
- Windows 11 dev machine (owner's primary per STATE.md)
- Node 23.8.0 (confirmed from RESEARCH.md Environment Availability)
- pnpm 10.x installed
- Real Microsoft account: eliyahu6666@outlook.com (per STATE.md) — confirmed owns Minecraft Java Edition
- Running via `pnpm --filter ./launcher dev` in dev mode (Vite HMR active — fine for QA)
- macOS verification is DEFERRED to Phase 7 clean-machine test (per RESEARCH.md Environment Availability)

**Test checklist (from docs/MANUAL-QA-auth.md, populated in Plan 00):**

1. Test 1: Happy-path login (AUTH-01, AUTH-02, AUTH-05)
2. Test 2: Cancel mid-flow (D-07)
3. Test 3: Filesystem token-leak audit (AUTH-04)
4. Test 4: 7-day refresh (AUTH-02 SC2) — time-sensitive, can overlap into Phase 3
5. Test 5: XSTS error surfaces (AUTH-03) — only reproducible codes
6. Test 6: Logout + re-login (AUTH-06)

**Expected phase complete signal:**

All 6 checklist items signed off in the `## Sign-off` table of `docs/MANUAL-QA-auth.md`. Test 4 (7-day) can be marked `Deferred-to-Phase-3` with explanation if time does not permit; Tests 1, 2, 3, 5, 6 are non-deferrable.

**Defect routing:**

If any test fails, do NOT amend PLAN files. Log the failure, open a gap-closure situation (`/gsd:plan-phase --gaps` when returning), which will create a new Plan 07 closing the specific gap.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verification — MCE approval + full live-endpoint QA walkthrough</name>

  <files>docs/MANUAL-QA-auth.md, docs/azure-app-registration.md</files>

  <action>
    Human walkthrough of the 6 live-endpoint tests in `docs/MANUAL-QA-auth.md`. This is a checkpoint task — see `<how-to-verify>` for the full procedure. Claude does NOT automate this; the user runs the launcher, authenticates against real Microsoft endpoints, and populates the sign-off table at the bottom of MANUAL-QA-auth.md.
  </action>

  <what-built>
    Phase 2 implementation complete through Plans 00-05:
    - Launcher builds (`pnpm --filter ./launcher build` clean)
    - Full vitest suite green (AuthManager + IPC + xstsErrors + redact + safeStorageCache + authStore + Zustand store + LoginScreen + LoadingScreen + ErrorBanner + DeviceCodeModal + AccountBadge + useSkinHead)
    - Main-process auth state machine (AuthManager) wired to prismarine-auth with flow:'msal', Azure AD client id 60cbce02-072b-4963-833d-edb6f5badc2a, safeStorage-encrypted cache, atomic writes, XSTS error mapping, silent refresh gated before renderer mounts, logout wipes cache dir
    - Renderer state machine (Zustand) routes between LoginScreen / LoadingScreen / Play-forward based on auth state; DeviceCodeModal + ErrorBanner + AccountBadge render per UI-SPEC locked copy
    - electron-log hook redacts JWT / refresh_token / access_token / accessToken patterns before write
    - docs/MANUAL-QA-auth.md has the 6-test checklist template ready to sign off
  </what-built>

  <how-to-verify>
    **Step 0 — MCE approval check (BLOCKING):**

    Before starting any test, open `docs/azure-app-registration.md` and confirm a populated `MCE approval received: <DATE>` line. If missing, check email for Microsoft's approval notification. If no email:

    1. Do NOT proceed with Test 1.
    2. Wait for the approval email (expected window 2026-04-21 to 2026-04-27 per STATE.md; if overdue, contact reviewer via the original thread).
    3. When the email arrives, edit `docs/azure-app-registration.md`: add under the existing registration record a new line: `**MCE approval received:** <YYYY-MM-DD>` with the email's date.
    4. Then proceed to Test 1.

    **Environment setup:**

    1. In a terminal at the repo root: `pnpm --filter ./launcher dev`. Wait for the Electron window to open. Confirm the console shows `Security audit: { ..., allTrue: true }`.
    2. If a previous test left state behind, wipe user data first: close the launcher, then on Windows run `Remove-Item -Recurse -Force "$env:APPDATA\Wiiwho"` (PowerShell) or `rm -rf "$APPDATA/Wiiwho"` (bash). This ensures a clean first-run.
    3. Keep DevTools open on the launcher window (Ctrl+Shift+I) throughout — watch the console for any SECURITY assertions or unhandled errors.

    **Run each test in order from `docs/MANUAL-QA-auth.md`:**

    Follow the steps already documented in MANUAL-QA-auth.md (created in Plan 00). As each test passes, append the result to the `## Sign-off` table at the bottom of that file, filling in the Date + Result + Notes columns. For each test:

    - **Test 1 Happy-path login:** Record the full username + UUID displayed. Confirm nothing in DevTools console contains JWT strings.
    - **Test 2 Cancel mid-flow:** Run twice — once with the button click, once with ESC. Confirm no unhandled promise rejections in DevTools.
    - **Test 3 Filesystem token-leak audit:** Run the `Select-String` / `grep` commands. PASTE any matches into Notes (should be zero).
    - **Test 4 7-day refresh:** If time permits, run with system clock manipulation. If not, note `Deferred to Phase 3 clean-machine test` in the Result column — acceptable per RESEARCH.md Environment Availability.
    - **Test 5 XSTS error surfaces:** At minimum verify 2148916233 (no Xbox profile) with a fresh MS account that has never touched xbox.com. Other codes are documented as reproducible-via-unit-tests-only in MANUAL-QA-auth.md — note that fact.
    - **Test 6 Logout + re-login:** Verify the Log out menu item is instant (no confirm dialog per D-15). Verify filesystem audit after logout shows no token files.

    **Reporting format in the `## Sign-off` table:**

    Populate each row with: `{Date YYYY-MM-DD}` | `PASS` or `FAIL` | notes. For failures, add a detailed note explaining what was observed and what was expected. Example row:

    ```
    | 1 happy-path | 2026-04-28 | PASS | Logged in as Eliyahu6666 (UUID: 550e8400...) in ~8s; Play-forward with AccountBadge top-right |
    ```

    **Commit the sign-off:**

    After all 6 rows are populated, commit via:

    ```
    git add docs/MANUAL-QA-auth.md docs/azure-app-registration.md
    ```

    Then create a commit with message `docs(02): live-endpoint QA sign-off` in a new commit (NOT amending).

    **If any test fails:**

    - Do NOT edit Plans 00-05.
    - Log the specific failure mode in MANUAL-QA-auth.md's sign-off row (what was expected vs what happened).
    - Return to the orchestrator with a gap description; the orchestrator will invoke `/gsd:plan-phase --gaps` to create a Plan 07 closing the failure.
  </how-to-verify>

  <verify>
    <automated>grep -qE "MCE approval received: 202[6-9]-" docs/azure-app-registration.md && grep -cE "^\| [1-6] " docs/MANUAL-QA-auth.md | (read n; test "$n" -ge 6 && echo "OK 6 sign-off rows" || echo "FAIL only $n sign-off rows")</automated>
  </verify>

  <done>6 sign-off rows populated in docs/MANUAL-QA-auth.md (each with Date + Result); MCE approval date recorded in docs/azure-app-registration.md; commit landed.</done>

  <resume-signal>Paste the completed `## Sign-off` table (6 rows) from docs/MANUAL-QA-auth.md, OR describe the specific test that failed including expected-vs-observed behavior. Owner may also say "MCE not yet approved — pausing" if the approval email has not arrived; phase status stays unchanged in that case.</resume-signal>
</task>

</tasks>

<verification>
1. `docs/azure-app-registration.md` contains a line matching `MCE approval received: 202[6-9]-`
2. `docs/MANUAL-QA-auth.md` `## Sign-off` table has 6 populated rows (no empty Date cells)
3. At least 5 of the 6 rows have `PASS` in the Result column (Test 4 may be `Deferred to Phase 3`)
4. `git log -1 --format=%s docs/MANUAL-QA-auth.md` returns `docs(02): live-endpoint QA sign-off` or similar
</verification>

<success_criteria>
- All 6 manual QA tests either pass OR (for Test 4 only) are deferred to Phase 3 with explicit note
- No token substrings (`eyJ`, `refresh_token`, `access_token`) found in filesystem audit
- Logout wipes token files and returns to LoginScreen without confirm
- Silent refresh on relaunch lands in Play-forward without prompting
- docs/MANUAL-QA-auth.md sign-off table committed
- Any discovered defects logged as gap items, NOT silently resolved
</success_criteria>

<output>
After completion, create `.planning/phases/02-microsoft-authentication/02-06-SUMMARY.md` documenting:
- MCE approval date
- Real Microsoft account tested against
- Actual username + UUID shape observed (first few chars; do NOT paste a full token)
- Test 1 end-to-end duration (click login → Play-forward)
- Test 3 filesystem audit: exact command run + output line count (0 expected)
- Whether Test 4 (7-day refresh) ran or deferred
- Which XSTS codes were reproduced (expected: at least 2148916233)
- Any observed UX issues that are not blockers but should be tracked for v0.2
- Total phase status: Phase 2 COMPLETE / BLOCKED on approval / BLOCKED on defect
</output>
