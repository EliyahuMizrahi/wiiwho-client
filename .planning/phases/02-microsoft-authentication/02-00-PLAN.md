---
phase: 02-microsoft-authentication
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - launcher/package.json
  - launcher/pnpm-lock.yaml
  - launcher/src/renderer/src/components/ui/dialog.tsx
  - launcher/src/renderer/src/components/ui/dropdown-menu.tsx
  - launcher/vitest.config.ts
  - docs/MANUAL-QA-auth.md
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06]
must_haves:
  truths:
    - "prismarine-auth, @azure/msal-node, and electron-log are installed in launcher"
    - "shadcn Dialog and DropdownMenu components exist under components/ui/"
    - "vitest config exists with node and jsdom environment split"
    - "MANUAL-QA-auth.md exists documenting live-endpoint verification steps"
  artifacts:
    - path: "launcher/package.json"
      provides: "new runtime deps"
      contains: "prismarine-auth"
    - path: "launcher/src/renderer/src/components/ui/dialog.tsx"
      provides: "shadcn Dialog component"
    - path: "launcher/src/renderer/src/components/ui/dropdown-menu.tsx"
      provides: "shadcn DropdownMenu component"
    - path: "launcher/vitest.config.ts"
      provides: "vitest node+jsdom environment configuration"
    - path: "docs/MANUAL-QA-auth.md"
      provides: "live endpoint verification checklist for AUTH-01..06"
  key_links:
    - from: "launcher/src/main/auth/* (future)"
      to: "prismarine-auth, @azure/msal-node, electron-log"
      via: "package.json dependency entries"
      pattern: "prismarine-auth|@azure/msal-node|electron-log"
---

<objective>
Install the Phase 2 dependency stack and scaffold shared test + QA infrastructure.

Purpose: Later plans can import the libraries directly, add unit tests against a working vitest config, and the phase can be verified end-to-end by walking the manual QA checklist once the MCE approval arrives.
Output: `launcher/package.json` updated with 3 runtime deps, 2 shadcn component files committed, `launcher/vitest.config.ts` with dual-environment setup, `docs/MANUAL-QA-auth.md` with the live-endpoint test matrix.
</objective>

<execution_context>
@C:/Users/Eliyahu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eliyahu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-microsoft-authentication/02-CONTEXT.md
@.planning/phases/02-microsoft-authentication/02-RESEARCH.md
@.planning/phases/02-microsoft-authentication/02-UI-SPEC.md
@.planning/phases/02-microsoft-authentication/02-VALIDATION.md
@launcher/package.json
@launcher/src/renderer/src/components/ui/button.tsx

<interfaces>
Existing Phase 1 IPC surface (FROZEN — Phase 2 does NOT extend this):

```typescript
// launcher/src/renderer/src/wiiwho.d.ts
interface WiiWhoAPI {
  auth: {
    status: () => Promise<{ loggedIn: boolean; username?: string; uuid?: string }>
    login: () => Promise<{ ok: boolean; username?: string; error?: string }>
    logout: () => Promise<{ ok: boolean }>
    onDeviceCode: (cb: (p: {
      userCode: string
      verificationUri: string
      expiresInSec: number
    }) => void) => () => void
  }
  // ... game, settings, logs, __debug (unchanged)
}
```

Currently installed versions (from launcher/package.json):
- electron: ^39.2.6
- react: ^19.2.1
- zustand: ^5.0.12
- radix-ui: ^1.4.3
- lucide-react: ^1.8.0
- tailwindcss: ^4.2.3
- vitest: ^4.1.4
- @electron-toolkit/preload, @electron-toolkit/utils present

Target install versions (pinned per RESEARCH.md §Standard Stack verified 2026-04-21):
- @azure/msal-node: ^5.1.3 (note CLAUDE.md says 4.x; live latest is 5.1.3, no breaking changes for device-code flow surface)
- prismarine-auth: ^3.1.1
- electron-log: ^5.4.3
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Phase 2 runtime dependencies and shadcn components</name>
  <files>launcher/package.json, launcher/pnpm-lock.yaml, launcher/src/renderer/src/components/ui/dialog.tsx, launcher/src/renderer/src/components/ui/dropdown-menu.tsx</files>
  <read_first>
    - launcher/package.json (see current dep tree; Phase 2 additions must not collide with existing names)
    - launcher/src/renderer/src/components/ui/button.tsx (existing shadcn component — mirror its style/conventions for dialog + dropdown-menu)
    - .planning/phases/02-microsoft-authentication/02-RESEARCH.md lines 96-130 (Standard Stack versions, exact install commands)
    - .planning/phases/02-microsoft-authentication/02-UI-SPEC.md lines 33-42 (shadcn component table — dialog + dropdown-menu to add)
  </read_first>
  <action>
    From the repo root run three install steps, sequentially.

    Step 1: Add runtime deps. Run:
    ```
    pnpm --filter ./launcher add @azure/msal-node@^5.1.3 prismarine-auth@^3.1.1 electron-log@^5.4.3
    ```
    This writes 3 new entries under `dependencies` in `launcher/package.json` and updates `launcher/pnpm-lock.yaml`. Do NOT add these to devDependencies.

    Step 2: Add shadcn Dialog component. Run from the `launcher/` directory:
    ```
    npx shadcn@latest add dialog
    ```
    This creates `launcher/src/renderer/src/components/ui/dialog.tsx`. If the CLI prompts for confirmation, answer yes. If the CLI reports the component already exists, skip Step 2.

    Step 3: Add shadcn DropdownMenu component. Run from the `launcher/` directory:
    ```
    npx shadcn@latest add dropdown-menu
    ```
    This creates `launcher/src/renderer/src/components/ui/dropdown-menu.tsx`. Same skip rule.

    Do NOT install `keytar`, `node-keytar`, `@azure/msal-browser`, `winston`, `node-pty`, or `got` — RESEARCH.md §Don't Hand-Roll explicitly bans these for Phase 2 paths.

    If Step 1 fails with a Node.js version error (owner's dev machine runs Node 23.8.0 per RESEARCH.md Environment Availability), proceed anyway and note the warning in the summary — install should still succeed; the advisory is about @electron-toolkit compatibility.

    After all three steps, run `pnpm --filter ./launcher install` once more to lock.
  </action>
  <verify>
    <automated>cd launcher && node -e "const p=require('./package.json');['@azure/msal-node','prismarine-auth','electron-log'].forEach(d=>{if(!p.dependencies[d])throw new Error('missing '+d);});console.log('OK',Object.keys(p.dependencies).filter(d=>['@azure/msal-node','prismarine-auth','electron-log'].includes(d)).join(','))" && test -f src/renderer/src/components/ui/dialog.tsx && test -f src/renderer/src/components/ui/dropdown-menu.tsx && echo "OK all artifacts present"</automated>
  </verify>
  <acceptance_criteria>
    - `launcher/package.json` contains `"@azure/msal-node"` in the `dependencies` block with version starting `^5.`
    - `launcher/package.json` contains `"prismarine-auth"` in the `dependencies` block with version starting `^3.`
    - `launcher/package.json` contains `"electron-log"` in the `dependencies` block with version starting `^5.`
    - File `launcher/src/renderer/src/components/ui/dialog.tsx` exists AND contains the string `DialogContent` (shadcn's exported component name)
    - File `launcher/src/renderer/src/components/ui/dropdown-menu.tsx` exists AND contains the string `DropdownMenuContent`
    - `launcher/package.json` does NOT contain `"keytar"`, `"@azure/msal-browser"`, `"winston"`, or `"node-pty"` (RESEARCH.md-banned deps)
    - `pnpm-lock.yaml` has been updated (git diff shows changes under `importers: launcher`)
  </acceptance_criteria>
  <done>Three runtime deps installed, two shadcn components scaffolded from the official registry, lockfile updated. No banned dep names appear in launcher/package.json.</done>
</task>

<task type="auto">
  <name>Task 2: Add vitest config with node+jsdom env split and scaffold manual QA doc</name>
  <files>launcher/vitest.config.ts, docs/MANUAL-QA-auth.md</files>
  <read_first>
    - launcher/package.json (confirms vitest 4.1.4 installed; no current vitest.config.ts)
    - launcher/src/main/ipc/auth.test.ts (existing test using vi.mock('electron') — main-process test that should run in `node` env)
    - launcher/src/main/ipc/security.test.ts and game.test.ts and settings.test.ts (confirm existing main tests run under the same node pattern)
    - launcher/src/renderer/src/env.d.ts (current renderer type reference)
    - .planning/phases/02-microsoft-authentication/02-VALIDATION.md (entire file — test infrastructure + manual-only verifications inform the QA doc)
    - .planning/phases/02-microsoft-authentication/02-RESEARCH.md lines 825-870 (Validation Architecture — test map + Wave 0 gaps)
  </read_first>
  <action>
    Create TWO files.

    File 1: `launcher/vitest.config.ts` — vitest configuration that splits test environments by file path. Main-process tests (`src/main/**`) run in node; renderer-process tests (`src/renderer/**`) run in jsdom. Write exactly:

    ```typescript
    import { defineConfig } from 'vitest/config'
    import react from '@vitejs/plugin-react'
    import { resolve } from 'node:path'

    export default defineConfig({
      plugins: [react()],
      resolve: {
        alias: {
          '@': resolve(__dirname, 'src/renderer/src')
        }
      },
      test: {
        environmentMatchGlobs: [
          ['src/renderer/**', 'jsdom'],
          ['src/main/**', 'node'],
          ['src/preload/**', 'node']
        ],
        globals: false,
        include: ['src/**/*.{test,spec}.{ts,tsx}']
      }
    })
    ```

    Note: vitest 4.x still supports `environmentMatchGlobs` as a function-equivalent; if the runner prints a deprecation warning pointing to `test.projects`, ignore it — Phase 2 does not refactor to projects. The existing `auth.test.ts` (main) uses `vi.mock('electron')` and expects node env, which this config satisfies.

    If `jsdom` is not installed (it will not be — it is not yet a devDep), run this AFTER writing the config:
    ```
    pnpm --filter ./launcher add -D jsdom@^25
    ```
    Also install `@testing-library/react@^16` and `@testing-library/jest-dom@^6` as devDeps — future renderer component tests in later plans need them:
    ```
    pnpm --filter ./launcher add -D jsdom@^25 @testing-library/react@^16 @testing-library/jest-dom@^6 @testing-library/user-event@^14
    ```

    File 2: `docs/MANUAL-QA-auth.md` — live-endpoint verification checklist gated on MCE approval. The file MUST contain the following sections verbatim, in order:

    ```markdown
    # Phase 2 — Manual QA: Microsoft Authentication

    > Live-endpoint verification procedure. Runs AFTER automated tests pass AND after MCE approval email arrives for Azure AD app `60cbce02-072b-4963-833d-edb6f5badc2a`.

    ## Prerequisites

    - [ ] MCE approval email received (check `docs/azure-app-registration.md` for the approval-received date — blocked until populated)
    - [ ] Launcher runs via `pnpm --filter ./launcher dev` on the test machine
    - [ ] Owner has access to at least one real Microsoft account that owns Minecraft Java Edition
    - [ ] Filesystem tooling available: `grep` or PowerShell `Select-String` for the token-leak audit
    - [ ] Owner has admin control of a secondary Microsoft test account WITHOUT an Xbox profile (used for XSTS 2148916233 reproduction)

    ## Test 1 — Happy-path login (AUTH-01, AUTH-02, AUTH-05)

    1. Launch dev build. First screen SHOULD be LoginScreen (wordmark + "Log in with Microsoft" button + `v0.1.0-dev`).
    2. Click "Log in with Microsoft".
    3. DeviceCodeModal SHOULD appear within 3 seconds showing an 8-char user code, "Copy code" button, "Open in browser" button, "Stop signing in" button, and an `Expires in {mm:ss}` countdown.
    4. Click "Open in browser". Verify the system browser opens `https://www.microsoft.com/link` (or equivalent verification_uri from Microsoft).
    5. Enter the code, authenticate with the primary MS account, approve the `Wiiwho Client` app.
    6. Return to the launcher. Within 10 seconds, the modal SHOULD close and the UI SHOULD transition to the Play-forward layout with the top-right AccountBadge displaying the Minecraft username.
    7. Click the AccountBadge. Dropdown SHOULD show full username, full 32-char UUID, and "Log out".

    Pass criteria: steps 1-7 complete without errors. The console logs contain no JWT tokens (check via DevTools console; should be clean).

    ## Test 2 — Cancel mid-flow (D-07)

    1. Click "Log in with Microsoft" → modal opens with device code.
    2. Click "Stop signing in". Modal SHOULD close within 1 second and return to LoginScreen.
    3. Repeat, but press `Escape` instead of clicking. Same outcome.

    Pass criteria: no hung polling promise, no second modal appears, console does not print unhandled promise rejections.

    ## Test 3 — Filesystem token-leak audit (AUTH-04)

    After Test 1, with the launcher still running and the user logged in, run:

    Windows (PowerShell):
    ```
    Select-String -Path "$env:APPDATA\Wiiwho\*" -Pattern "eyJ|refresh_token|access_token" -Recurse
    ```

    macOS (bash):
    ```
    grep -rE "eyJ|refresh_token|access_token" "$HOME/Library/Application Support/Wiiwho/"
    ```

    Pass criteria: ZERO matches. Any match is an AUTH-04 blocker.

    Also run:
    ```
    grep -rE "eyJ|refresh_token|access_token" ~/.config/Wiiwho/logs/  # Linux path if present
    ```
    Pass criteria: zero matches.

    ## Test 4 — 7-day refresh (AUTH-02 Success Criterion 2)

    Time-sensitive; run across two sessions.

    Session A (day 0):
    1. Complete Test 1 (fresh login).
    2. Fully close the launcher (quit — not just window close on macOS).
    3. Record date/time.

    Session B (day 7):
    1. Reopen the launcher.
    2. LoadingScreen SHOULD appear briefly, then transition to Play-forward with the same user logged in.
    3. At NO point should the LoginScreen or DeviceCodeModal appear.

    If day 7 is impractical, use system clock manipulation: set the OS clock forward 7 days between Session A and Session B. Restore the clock afterward.

    Pass criteria: silent refresh succeeds without re-prompting.

    ## Test 5 — XSTS error surfaces (AUTH-03)

    Reproducibility notes per code:

    - `2148916233` (no Xbox profile): use a fresh MS account that has never accessed xbox.com. Walking through MS sign-in will trigger this.
    - `2148916235` (country blocked): requires a VPN + MS account from a blocked country; DEFER to post-v0.1 unless reproducible.
    - `2148916236` / `2148916237` (age verification): requires a real-account-state; DEFER unless reproducible.
    - `2148916238` (child not in Family): requires a sub-18 MS account; DEFER unless reproducible.

    For each reproducible code, attempt login. Expected: ErrorBanner appears under the login button with plain-English message per UI-SPEC §ErrorBanner and a Help link that opens in the system browser.

    For non-reproducible codes, verify via the unit test fixtures in `launcher/src/main/auth/__tests__/xstsErrors.test.ts` (must pass).

    Pass criteria: every reproducible code shows the correct message; Help button opens the correct URL; unit tests cover the non-reproducible codes.

    ## Test 6 — Logout + re-login (AUTH-06)

    1. Starting logged-in, click AccountBadge → "Log out".
    2. UI SHOULD transition to LoginScreen instantly (no confirm dialog, per D-15).
    3. Run the Test 3 filesystem audit — there should be NO token files remaining under `auth/` for the logged-out account (the pointer file may still exist with an empty `accounts[]` array and cleared `activeAccountId`).
    4. Click "Log in with Microsoft". Complete device-code flow as in Test 1.
    5. Relaunch the launcher (full quit + reopen). LoadingScreen → Play-forward with the same account.

    Pass criteria: logout wipes tokens; re-login succeeds; silent refresh works on subsequent launch.

    ## Sign-off

    | Test | Date | Result | Notes |
    |------|------|--------|-------|
    | 1 happy-path | | | |
    | 2 cancel | | | |
    | 3 token-leak audit | | | |
    | 4 7-day refresh | | | |
    | 5 XSTS errors | | | |
    | 6 logout + re-login | | | |

    All 6 tests must pass before the phase is marked complete.
    ```

    Keep `docs/MANUAL-QA-auth.md` exactly as written above — later plans reference it and the table headers are load-bearing for sign-off tracking.
  </action>
  <verify>
    <automated>test -f launcher/vitest.config.ts && test -f docs/MANUAL-QA-auth.md && grep -q "environmentMatchGlobs" launcher/vitest.config.ts && grep -q "Test 3 — Filesystem token-leak audit" docs/MANUAL-QA-auth.md && grep -q "Test 4 — 7-day refresh" docs/MANUAL-QA-auth.md && cd launcher && pnpm run test:run 2>&1 | tail -20 | grep -qE "(no tests|PASS|0 failed)"</automated>
  </verify>
  <acceptance_criteria>
    - File `launcher/vitest.config.ts` exists and contains string `environmentMatchGlobs`
    - File `launcher/vitest.config.ts` contains `['src/main/**', 'node']` (node env for main tests)
    - File `launcher/vitest.config.ts` contains `['src/renderer/**', 'jsdom']` (jsdom env for renderer tests)
    - File `launcher/vitest.config.ts` contains `'@': resolve(__dirname, 'src/renderer/src')` (path alias)
    - File `docs/MANUAL-QA-auth.md` exists
    - `docs/MANUAL-QA-auth.md` contains each of the 6 test headers verbatim: `## Test 1 — Happy-path login`, `## Test 2 — Cancel mid-flow`, `## Test 3 — Filesystem token-leak audit`, `## Test 4 — 7-day refresh`, `## Test 5 — XSTS error surfaces`, `## Test 6 — Logout + re-login`
    - `docs/MANUAL-QA-auth.md` contains the Azure AD app id `60cbce02-072b-4963-833d-edb6f5badc2a`
    - `docs/MANUAL-QA-auth.md` contains the JWT grep pattern `eyJ|refresh_token|access_token`
    - `launcher/package.json` now lists `jsdom`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event` under `devDependencies`
    - Running `cd launcher && pnpm run test:run` exits with code 0 and the existing 4 test files still pass under the new config
  </acceptance_criteria>
  <done>vitest.config.ts wires dual-env for main+renderer tests; existing Phase 1 tests still pass; MANUAL-QA-auth.md is the single source of truth for live-endpoint verification and lists all 6 test cases from VALIDATION.md.</done>
</task>

</tasks>

<verification>
1. `cd launcher && pnpm run test:run` exits 0 — all Phase 1 tests still green under new config
2. `cd launcher && pnpm run typecheck` exits 0 — new deps resolve without TS errors
3. `grep -E "@azure/msal-node|prismarine-auth|electron-log" launcher/package.json` returns 3 lines
4. `ls launcher/src/renderer/src/components/ui/` lists `button.tsx`, `dialog.tsx`, `dropdown-menu.tsx`
5. `grep -c "^## Test " docs/MANUAL-QA-auth.md` returns `6`
</verification>

<success_criteria>
- 3 runtime deps installed (@azure/msal-node, prismarine-auth, electron-log) + 4 test devDeps (jsdom, @testing-library/*)
- 2 shadcn components committed (dialog, dropdown-menu)
- vitest.config.ts configures node+jsdom env split
- docs/MANUAL-QA-auth.md covers all 6 VALIDATION.md test cases with reproducibility notes
- No banned deps introduced
</success_criteria>

<output>
After completion, create `.planning/phases/02-microsoft-authentication/02-00-SUMMARY.md` documenting:
- Installed versions (actual, from pnpm-lock.yaml — not just the caret ranges)
- Any CLI prompts encountered during shadcn add
- Note on Node.js version on dev machine if pnpm install produced warnings
- Confirmation that all Phase 1 tests still pass under the new vitest config
</output>
