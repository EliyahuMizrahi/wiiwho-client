# Dependencies
node_modules/

# Build output
dist/
out/
.vite/
# Note: launcher/build/ holds electron-builder icons/entitlements, NOT build output — tracked.

# OS / editor
.DS_Store
.vscode/*
!.vscode/extensions.json
Thumbs.db

# Logs
*.log*
.eslintcache

# Local env / secrets
.env
.env.local

# Claude Code local settings
.claude/

# Local GSD helper scripts
.planning/scripts/

# Phase 3: Temurin/Azul JRE binaries fetched at build time — NEVER commit.
launcher/resources/jre/
# Phase 3: Wiiwho mod jar built from client-mod at build time — NEVER commit.
launcher/resources/mod/

