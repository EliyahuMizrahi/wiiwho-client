/**
 * @vitest-environment jsdom
 *
 * Plan 04-00 Wave 0 scaffold — Sidebar component tests.
 *
 * Plan 04-02 (sidebar + main area) fills this in with real
 * assertions for navigation items, active-state, and responsive collapse.
 */

import { describe, it, afterEach } from 'vitest'
import { cleanup } from '@testing-library/react'

describe('Sidebar — Wave 0 scaffold', () => {
  afterEach(cleanup)
  it.todo('Plan 04-02 will implement real tests here (nav items + active state + collapse)')
})
