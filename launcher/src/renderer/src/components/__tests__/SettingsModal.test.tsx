/**
 * @vitest-environment jsdom
 *
 * Plan 04-00 Wave 0 scaffold — SettingsModal chrome tests.
 *
 * Plan 04-03 (Settings modal chrome) fills this in with real
 * assertions for open/close, pane routing, and focus trap.
 */

import { describe, it, afterEach } from 'vitest'
import { cleanup } from '@testing-library/react'

describe('SettingsModal — Wave 0 scaffold', () => {
  afterEach(cleanup)
  it.todo('Plan 04-03 will implement real tests here (modal chrome + pane routing + focus trap)')
})
