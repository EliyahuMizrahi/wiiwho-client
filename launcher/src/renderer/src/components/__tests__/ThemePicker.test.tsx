/**
 * @vitest-environment jsdom
 *
 * Plan 04-00 Wave 0 scaffold — ThemePicker component tests.
 *
 * Plan 04-04 (Settings modal Appearance pane) fills this in with real
 * assertions for accent swatches, selected-state, and keyboard navigation.
 */

import { describe, it, afterEach } from 'vitest'
import { cleanup } from '@testing-library/react'

describe('ThemePicker — Wave 0 scaffold', () => {
  afterEach(cleanup)
  it.todo('Plan 04-04 will implement real tests here (swatch grid + selected state + a11y)')
})
