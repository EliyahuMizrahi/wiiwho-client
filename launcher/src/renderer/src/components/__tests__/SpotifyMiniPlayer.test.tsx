/**
 * @vitest-environment jsdom
 *
 * Plan 04-00 Wave 0 scaffold — SpotifyMiniPlayer component tests.
 *
 * Plan 04-06 (Spotify UI) fills this in with real assertions for
 * now-playing display, playback controls, and disconnected state.
 */

import { describe, it, afterEach } from 'vitest'
import { cleanup } from '@testing-library/react'

describe('SpotifyMiniPlayer — Wave 0 scaffold', () => {
  afterEach(cleanup)
  it.todo('Plan 04-06 will implement real tests here (now-playing + controls + disconnected state)')
})
