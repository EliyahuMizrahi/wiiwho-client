/**
 * @vitest-environment jsdom
 *
 * Plan 04-00 Wave 0 scaffold — settings theme store tests.
 *
 * Plan 04-01 (settings v2 migration + setAccent) fills this in with real
 * assertions for accent-color persistence, theme toggle, and v2 migration.
 */

import { describe, it, afterEach } from 'vitest'
import { cleanup } from '@testing-library/react'

describe('settings theme store — Wave 0 scaffold', () => {
  afterEach(cleanup)
  it.todo('Plan 04-01 will implement real tests here (setAccent + theme state + v2 migration)')
})
