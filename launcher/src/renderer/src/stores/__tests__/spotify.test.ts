/**
 * @vitest-environment jsdom
 *
 * Plan 04-00 Wave 0 scaffold — renderer Spotify store tests.
 *
 * Plan 04-06 (renderer spotify store + mini-player) fills this in with real
 * assertions for the now-playing polling state machine and control actions.
 */

import { describe, it, afterEach } from 'vitest'
import { cleanup } from '@testing-library/react'

describe('renderer spotify store — Wave 0 scaffold', () => {
  afterEach(cleanup)
  it.todo('Plan 04-06 will implement real tests here (now-playing polling + play/pause/next)')
})
