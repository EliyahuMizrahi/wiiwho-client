/**
 * @vitest-environment jsdom
 *
 * Plan 04-00 Wave 0 scaffold — motion token catalog tests.
 *
 * Plan 04-01 (token catalog + motion tokens) fills this in with real
 * assertions against the motion duration/easing token surface.
 */

import { describe, it, afterEach } from 'vitest'
import { cleanup } from '@testing-library/react'

describe('motion tokens — Wave 0 scaffold', () => {
  afterEach(cleanup)
  it.todo('Plan 04-01 will implement real tests here (motion tokens + prefers-reduced-motion)')
})
