/**
 * Plan 04-00 Wave 0 scaffold — settings v1 → v2 migration tests.
 *
 * Plan 04-01 (settings v2 migration) fills this in with real assertions for
 * the v1-schema-on-disk → v2-in-memory migration (adds accent color + theme
 * prefs), per-field fallback on corruption, and the atomic temp+rename
 * write invariant carrying through to v2.
 *
 * @vitest-environment node
 */

import { describe, it } from 'vitest'

describe('settings store v2 migration — Wave 0 scaffold', () => {
  it.todo('Plan 04-01 will implement real tests here (v1 → v2 migration + per-field fallback)')
})
