# Spotify Test Fixtures

Test fixtures for Spotify OAuth and Web API mock responses. Populated in Plans 04-05 (main-process OAuth + API + token store) and 04-06 (renderer store + mini-player). Intentionally empty in Wave 0 — this directory exists so downstream plans have a stable location to drop fixture files without re-scaffolding.
