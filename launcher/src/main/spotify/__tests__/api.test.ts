/**
 * Plan 04-00 Wave 0 scaffold — Spotify Web API client tests.
 *
 * Plan 04-05 (main-process Spotify) fills this in with real assertions for
 * GET /v1/me/player/currently-playing, play/pause/next/previous, and
 * 401-on-expired-token refresh behavior.
 *
 * @vitest-environment node
 */

import { describe, it } from 'vitest'

describe('spotify web API client — Wave 0 scaffold', () => {
  it.todo('Plan 04-05 will implement real tests here (now-playing + controls + 401 refresh)')
})
