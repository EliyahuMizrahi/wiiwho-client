/**
 * Plan 04-00 Wave 0 scaffold — Spotify safeStorage token store tests.
 *
 * Plan 04-05 (main-process Spotify) fills this in with real assertions for
 * safeStorage-encrypted persistence at resolveSpotifyTokenPath(), the
 * fail-closed behavior when safeStorage is unavailable, and round-trip
 * encrypt/decrypt of the access/refresh token pair.
 *
 * @vitest-environment node
 */

import { describe, it } from 'vitest'

describe('spotify token store — Wave 0 scaffold', () => {
  it.todo('Plan 04-05 will implement real tests here (safeStorage encrypt + fail-closed)')
})
