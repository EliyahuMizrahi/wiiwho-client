/**
 * Plan 04-00 Wave 0 scaffold — Spotify OAuth PKCE flow tests.
 *
 * Plan 04-05 (main-process Spotify) fills this in with real assertions for
 * PKCE code-verifier generation, loopback server binding on
 * SPOTIFY_REDIRECT_PORTS, /authorize URL construction, and /token exchange.
 *
 * @vitest-environment node
 */

import { describe, it } from 'vitest'

describe('spotify oauth PKCE flow — Wave 0 scaffold', () => {
  it.todo('Plan 04-05 will implement real tests here (PKCE + loopback bind + token exchange)')
})
