// The preload bridge is declared in renderer-side types
// (`src/renderer/src/wiiwho.d.ts`). This file intentionally contains no
// `electron` / `api` globals — the renderer's only entry point is `window.wiiwho`.
export {}
